export enum ContentEnum {
  FULL = 'full',
  FIXED = 'fixed',
}

export enum ThemeEnum {
  LIGHT = 'light',
  DARK = 'dark',
}

/**
 * 权限模式
 */
export enum PermissionModeEnum {
  ROLE = 'ROLE',
  BACKEND_API = 'BACKEND',
  ROUTE_MAPPING = 'ROUTE',
}